#include <simplecpp>

main_program {
    double p, q;
    cin >> p >> q;
    int n;
    cin >> n;
    double w = (q - p) / n;
    double area = 0;
    for (int i = 0; i < n; i++) {
        area = area + w * (1 / (p + iw));
    }
    cout << area << endl;
    cout << log(x) << endl; // Taking the natural logarithm of the calculated area
}
